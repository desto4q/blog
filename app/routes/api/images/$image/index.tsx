// import { createClient } from "~/client/pocketbase";
// import type { Route } from "../../../home/+types";
// import type { RecordModel } from "pocketbase";

// interface Item extends RecordModel {}
// export let loader = async ({ params }: Route.LoaderArgs) => {
//   let { image } = params;

//   let db = createClient();
//   // let img_url = await db.getFileUrl(record, image);
//   return {
//     hello: "world",
//     image,
//   };
// };
